import { useState } from "react";


function App() {
	const [count, setCount] = useState(0);

	return (
		<div className="App">
			<p>I'm a remote app that a different teams works on</p>
		</div>
	);
}

export default App;
